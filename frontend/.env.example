# Frontend API base (optional).
# Local dev: leave unset — requests use /api and Vite proxies to the backend (vite.config.ts).
# Production (Netlify): set to your public FastAPI origin (no trailing slash):
# VITE_API_URL=https://your-api.example.com
